// Read input and write as column

String str;
int len, i = 0;

print("Please enter an input string: ");
str = System.console().readLine();

len = str.length();

while (len > i) {
		
	println(str.substring(len - (len - i), len - (len - i - 1)));

	i++;
	
}
